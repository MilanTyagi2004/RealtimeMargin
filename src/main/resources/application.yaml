spring:
  application:
    name: "liquidation-engine"

  datasource:
    url: "jdbc:mysql://localhost:3306/liquidation_engine"
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true